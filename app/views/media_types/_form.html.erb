<%= form_for(@media_type) do |f| %>
  <% if @media_type.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@media_type.errors.count, "error") %> prohibited this Media type from being saved:</h2>
    <ul>
    <% @media_type.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :abbreviation %><br />
    <%= f.text_field :abbreviation %>
  </div>
  <div class="field">
    <%= f.label :parent_id %><br />
    <%= f.select :parent_id, parent_options_tree(@media_types, @media_type) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
